# select a revision to deploy, available values: stable, beta, canary
AFFINE_REVISION={{ affine_revision }}

# set the port for the server container it will expose the server on
PORT={{ affine_ext_port }}

# set the host for the server for outgoing links
AFFINE_SERVER_HTTPS=true
AFFINE_SERVER_HOST={{ affine_ext_domain }}
# or 
# AFFINE_SERVER_EXTERNAL_URL=https://affine.yourdomain.com

# position of the database data to persist
DB_DATA_LOCATION={{ affine_root_dir }}/postgres/pgdata
# position of the upload data(images, files, etc.) to persist
UPLOAD_LOCATION={{ affine_root_dir }}/storage
# position of the configuration files to persist
CONFIG_LOCATION={{ affine_root_dir }}/config

# database credentials
DB_USERNAME={{ affine_db_user }}
DB_PASSWORD={{ affine_db_password }}
DB_DATABASE={{ affine_db_name }}
